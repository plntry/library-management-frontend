import { AliasToken } from "antd/es/theme/internal";

export interface ColorsType extends Partial<AliasToken> {
  bgWhite: string;
}

export const colors: ColorsType = {
  colorBgBase: "#ffffff",
  colorPrimary: "#f59600",
  bgWhite: "#ffffff",
  colorIcon: "#f59600",
};
